# 分类导航（已重组 2025）

> 文档已按 7 大类重组至 `docs/{linux,embedded,git,network,tools,programming,windows,cheatsheet}`，旧 `docs/posts` 已归档。Obsidian `dataview` 查询已同步更新。

## Linux 系统

```dataview
list
from "docs/linux"
```

- 发行版: `docs/linux/distro` — Arch / Gentoo / Ubuntu / LFS / Rock5B
- 文件系统: `docs/linux/filesystem` — ext4→Btrfs / 硬盘扩容 / 缩小分区
- 系统管理: `docs/linux/system` — 用户 / 网络排查 / 静态IP / 版本信息

## 嵌入式与内核

```dataview
list
from "docs/embedded"
```

- Buildroot: `docs/embedded/buildroot`
- Yocto/OpenWrt: `docs/embedded/yocto`
- 内核: `docs/embedded/kernel` — 内存 / 驱动 / ELF
- 硬件: `docs/embedded/hardware` — CMOS / imx8mm / Fan / openocd / gpio

## Git 专栏

```dataview
list
from "docs/git" where contains(file.tags, "git")
```

```dataview
list
from #git
```

## 网络与代理

```dataview
list
from "docs/network"
```

- `docs/network/proxy` — cloudflared / openvpn / wireguard / v2ray / trojan / clash

## 工具箱

```dataview
list
from "docs/tools"
```

- apt: `docs/tools/apt`
- ssh: `docs/tools/ssh`
- samba: `docs/tools/samba`
- 其他: `docs/tools/misc` — tmux / zsh / UEFI / vscode / wheel

## 编程

```dataview
list
from "docs/programming"
```

- C/C++: `docs/programming/c-cpp`
- Python: `docs/programming/python`
- Shell: `docs/programming/shell`

## Windows & 速查表

```dataview
list
from "docs/windows" or "docs/cheatsheet"
```

- Windows: `docs/windows` — 网络共享 / 右键菜单 / Chrome DoH
- 速查表: `docs/cheatsheet` — Markdown / DWM

---

## 旧查询（已废弃，仅保留参考）

```dataview
list
from #linux and #tool
```

```dataview
list
from #linux or #system
```
